// import express from 'express';
// import { adminLogin, getAdminProfile, updateAdminDetails, changeAdminPassword } from '../controllers/admin.controller.js';
// import {  admin } from '../middlewares/admin.middleware.js'; // Assuming you have these middlewares

// const router = express.Router();

// // Admin login route (no authentication needed)
// router.post('/login', adminLogin);

// // Admin profile route (protected route)
// router.get('/profile',  admin,getAdminProfile);

// // Update admin details (protected route)
// router.put('/profile',  admin, updateAdminDetails);

// // Change admin password (protected route)
// router.put('/change-password',  admin, changeAdminPassword);

// export default router;
